# Example environment variables for local development
# Set the API base URL (no trailing slash). Leave blank to use relative paths.
# By default the client will use the deployed API at https://wander-nest-ad3s.onrender.com
# To point to a local API during development, uncomment and set the following:
# VITE_API_BASE=http://localhost:8000

